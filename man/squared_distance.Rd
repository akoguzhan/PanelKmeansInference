% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{squared_distance}
\alias{squared_distance}
\title{Fast Squared Euclidean Distance Matrix}
\usage{
squared_distance(A, B)
}
\arguments{
\item{A}{A numeric matrix of size \code{n1 x d}}

\item{B}{A numeric matrix of size \code{n2 x d}}
}
\value{
A numeric \code{n1 x n2} matrix where entry \code{(i,j)} is the squared Euclidean distance between row \code{i} of \code{A} and row \code{j} of \code{B}.
}
\description{
Computes the squared Euclidean distance between each row of matrix \code{A} and each row of matrix \code{B}.
This is a faster alternative to \code{proxy::dist()} for standard K-means clustering.
}
\examples{
A <- matrix(rnorm(10 * 3), nrow = 10)
B <- matrix(rnorm(20 * 3), nrow = 20)
D <- squared_distance(A, B)
}
\keyword{internal}
